<!DOCTYPE html>
<html>
    <head>
        {% if title %}
        <title>{{title}}</title>
        {% else %}
        <title>Welcome</title>
        {% endif %}
    </head>
    <body>
        <div>
            <h1>Welcome</h1>
        </div>
        {% block content %}{% endblock %}
    </body>
</html>
